spring:
  application:
    name: libra-registry-service
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8100}

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0